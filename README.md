This repo is an experimental one where I play with code with respect to portfolio optimisation. The current code is unstable, so figure it out the plan is to build something from the scratch to know exactly where the model is failing. The initial code developed was weeks of work but as I kep adding on things, I could not debug what went wrong and where - if it is the data set, if it is the actor critic, or maybe using raw returns without normalised returns. Turns out the variational autoenocder had smoothened out causing only noise to be captured which in turn caused the wealth to explode. So the goal this time is to build a proper strucured one without any strucutre breaking as keep on adding layers as much as possible to achieve robust results in terms of convergence as compared to the baseline model. 
Later when there is sufficiently enough proven results,it will be published as a new branch in a public repo for active feedback 

Thank you for reading, Cheers !
